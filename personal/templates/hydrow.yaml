{{- if (index .Values "hydrow" "enable") }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hydrow
  namespace: argo
spec:
  destination:
    namespace: hydrow
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    path: helm/
    repoURL: {{ (index .Values "hydrow" "source" "repoURL") }}
    targetRevision: {{ (index .Values "hydrow" "source" "targetRevision") }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{- end }}